name: nutrifyngo
description: "NutrifynGo: seguimiento diario de calorias, agua, pasos y rutinas."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter

  image_picker: ^1.2.1
  home_widget: ^0.9.0
  shared_preferences: ^2.2.2
  flutter_local_notifications: ^21.0.0
  flutter_timezone: ^5.0.2
  timezone: ^0.11.0
  sqflite: ^2.0.4
  path: ^1.8.2
  fl_chart: ^0.68.0
  http: ^1.2.2
  provider: ^6.0.5
  cupertino_icons: ^1.0.8
  pedometer: ^4.0.1
  permission_handler: ^11.3.0
  google_nav_bar: ^5.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  path_provider: ^2.1.5

flutter:
  uses-material-design: true